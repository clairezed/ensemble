- content_for :current_navigation_section, :user_reports

.title-block
  h1 
    | Gestion des signalements

    / = link_to new_admin_user_report_path, class: "btn btn-info float-right" do
    /   i.fa.fa-plus>
    /   | Ajouter un événement
/ .card
/   .card-body
/     = form_tag admin_user_reports_path, method: :get, data: {is_filter_form: true} do

/       .lead
/         | Filtrer votre recherche
/       .row
/         .form-group.col-3
/           label.sr-only Par bloqueur
/           => text_field_tag 'by_title', params[:by_title], \
/             class: "form-control", placeholder: "par titre"

/         .form-group.col-3
/           = select_tag :by_state, \
/                 options_for_select(user_report_state_options, params[:by_state]), \
/                 prompt: "Tous les statuts",\
/                 class: "form-control", \
/                 onchange: "$('[data-is-filter-form]').submit()"

/         .form-group.col-5
/           = submit_tag "Appliquer", class: "btn btn-success"
/           = link_to admin_user_reports_path, class: "btn btn-sm" do
/             span.glyphicon.glyphicon-repeat>
/             | Réinitialiser


.card
  .card-body
    small.float-right = I18n.t("results", count: @user_reports.count)
    = yield

