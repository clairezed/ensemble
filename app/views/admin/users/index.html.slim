= render layout: 'layout_index' do 

  table.table.table-striped.table-hover.table-sm
    thead.thead-inverse
      tr
        th Nom
        th Prénom
        th Email
        th Inscription finalisée ?
        th 
    tbody
      - @users.each do |user| 
        tr
          td= user.lastname
          td= user.firstname
          td= user.email
          td= td= boolean_badge(user.registration_complete)
          td.action-btns
            => link_to edit_profile_admin_user_path(user), 
                        class: "btn btn-sm btn-info", 
                        title: "Editer" do

              i.fa.fa-pencil
            = link_to admin_user_path(user),
                      method: :delete, 
                      data: {confirm: "Confirmer la suppression ?"}, 
                      title: "Supprimer", 
                      class: "btn btn-sm btn-danger" do 

                i.fa.fa-trash
      
      - if @users.empty?
        tr
          td colspan="7"
            p.text-center: em.text-muted Aucun résultat


  = paginate @users